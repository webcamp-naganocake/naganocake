
<div class="row">
  	<div class="col-xs-2">
    <!-- ジャンルのサイドバーを表示 -->
    <%= render "customers/items/sidebar", genres: @genres %>
    </div>

    <div class="col-xs-9 col-xs-offset-1">
        <h5>ようこそ、NaganoCakeへ！<br>
        このサイトは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品の閲覧はできますが、<br>
        購入には会員登録が必要になります。<br>
        </h5>

        <h3 class="recommended-items">オススメ商品</h3>

        <!-- 商品を4つ表示するようにする -->
        <div class="col-xs-9 col-xs-offset-1">
            <div class="item--card-content">
                <% @items.first(4).each do |item| %>
                    <div class="col-xs-3" style="padding-right: 0px; padding-left: 0px;">
                        <%= link_to item_path(item.id) ,style: "color: black;" do %>
                            <%= attachment_image_tag item, :image, :fill, 100, 50, format: 'jpeg',fallback: "no_image.jpg" %><br>
                            <%= item.name %>
                            ¥<%= item.price_without_tax %>
                        <% end %>
                    </div>
                <% end %>
            <div>
        </div>

        
    </div>
    <h3><%= link_to "もっと見る", items_path, class: "pull-right", style: "font-family: HiraMinPro-W6; color: #000000;" %></h3>
</div>
